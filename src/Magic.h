/*
* This file includes magic values that we will likely eradicate long-term.
*
* Since this project is still being refactored, we'll put them here until it becomes clear which
* module should really host the macro, until we can eradicate all of them.
*/
namespace Shiro::Magic {
    static constexpr auto BUTTON_PRESSED_THIS_FRAME = 2;
}